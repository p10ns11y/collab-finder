# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Hire board — personal Google Sheet id/url (copy from config.example.json)
data/hire-board/config.local.json

# Personal contact / network exports (PII — never commit)
# Use data/connections.sample.csv + data/contacts.sample.csv for local demos
data/contacts.csv
data/connections.csv

# Personal mobility / visa / employer-outcome planning (never commit)
data/spacexai-critical-path-visa-plan.md
data/crtitical_path.pdf
data/crtitical_path_prep.pdf
data/distillation/public-projects-full.json

# Operator identity pack — local only (universe, places, CV, constraints). Seed via scripts/seed-operator-config.sh
data/operator/

# Rust test fixtures copied from operator pack (local; regenerate via seed script)
src-tauri/testdata/

# Local CRAP / complexity CI artifacts
.ci-venv-lizard/
lizard-rust.xml
crap-report.md
src-tauri/llvm-cov-summary.json

# Local scratch / screenshots
.tmp/
pipeline-export.json

# Impeccable: local/session (surfaces, live inject, mocks, critique). Keep on disk; do not commit.
.impeccable/

# Tesla careers dump (paste from cua-api/apps/careers/state — often Akamai-blocked live)
data/mission-firms/tesla.jobs.json

# Cursor local IDE wiring (symlinks to .agents — recreate per machine)
.cursor/

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# TypeScript / Vite build artifacts
*.tsbuildinfo
vite.config.d.ts
