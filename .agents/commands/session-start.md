# /session-start — collab-finder

Load session SoT, then work. Do not dump this file back as a wall of text.

## Do now

1. Read `~/life-os/Projects/collab-finder/README.md` (frontmatter: `next_action`, `review_date`, `progress`).
2. State in 2–4 bullets: current `next_action`, what this session will do, what it will **not** do.
3. If the user named a different goal, update the life-os note first, then proceed.
4. Verify SoT is root `AGENTS.md` — `pnpm type-check` / `pnpm verify` / `cargo test` / `pnpm gate`. There is no `pnpm lint` or `pnpm precommit`.
5. Load skills only when the task matches the AGENTS routing table. Fusion only if they said "ignite" or "use fusion".
