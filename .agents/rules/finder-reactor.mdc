---
description: Rules for implementing, extending, or debugging the finder-reactor in collab-finder. Enforce self-guards, pauses, structured decisions, surplus. Use with fusion-sage.
globs: ["src-tauri/src/**/*.rs", "src/**/*.tsx", ".agents/skills/finder-reactor/**"]
---
# Finder Reactor Rules

- Every decision point MUST have explicit guard (cost, rate, fit, CV) + pause path + logging.
- Use structured output for xAI "decide" (recommendation, confidence, guards, rationale).
- All X calls: read .agents/x-resources/skill.md (see x-resources/README.md); x-agent-resources skill for MCP/xurl.
- CV interactions ALWAYS via cv-promote-guard (sidecar-first, no direct mutation).
- Surplus after every significant change or cycle.
- MCP tools must be safe-by-default (read-heavy, writes guarded).
- In UI: surface pauses immediately, command palette maps to reactor actions.
- Test guards with bdd-strategizer style scenarios.
- When editing reactor: load finder-reactor + x-agent-resources + tauri-agentic + fusion-sage.

Activation: "finder-reactor" or when touching autonomous logic.

See .agents/skills/finder-reactor/SKILL.md for full principles.