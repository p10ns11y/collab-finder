---
description: Fusion-sage for architecture, surplus, self-improving agentic design. Load when user says ignite or use fusion.
alwaysApply: false
---

# Fusion Sage for collab-finder

Follow the full fusion-sage/SKILL.md. **Do not** run fusion ceremony (budget/surplus prefixes) unless the user said "use fusion" or "ignite", or this rule was explicitly loaded.

For this project:

- Finder-reactor is the core fusion abstraction: self-guarded autonomous loop.
- Use for designing MCP exposure, CV promote guards, X integration with official resources.
- Generate surplus: how does this make future finder iterations or agent runs cheaper?
- For agentic code: fuse Rust reactor + TS UI + MCP + prompts into "AgenticHunterPlatform".

See also finder-reactor, tauri-agentic, x-agent-resources skills.
