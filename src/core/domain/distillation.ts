import profileStrategyMd from '../../../data/distillation/curation/profile-and-strategy.md?raw'
import operatorsMd from '../../../data/distillation/x-search/operators.md?raw'

/** In-app copy of distillation curation + operator rules (source files under `data/distillation/`). */
export const PROFILE_STRATEGY_MD = profileStrategyMd
export const X_OPERATORS_MD = operatorsMd
