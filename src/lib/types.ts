/** @deprecated Import from `core/domain/finder` — kept for transitional imports. */
export type {
  Tweet,
  Decision,
  ReactorState,
  SearchPreset,
} from '../core/domain/finder'

export {
  DEFAULT_SEARCH_QUERY,
  DEFAULT_CV_SUMMARY,
  SEARCH_PRESETS,
} from '../core/domain/finder'