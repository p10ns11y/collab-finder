name: CI

on:
  push:
    branches: [master, main, feat/**]
  pull_request:

concurrency:
  group: ci-${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  changes:
    runs-on: ubuntu-22.04
    timeout-minutes: 5
    outputs:
      app_source: ${{ steps.paths.outputs.app_source }}
    steps:
      - name: Checkout
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
        with:
          fetch-depth: 0

      - name: Detect app-source diff
        id: paths
        env:
          GITHUB_EVENT_NAME: ${{ github.event_name }}
          GITHUB_BASE_SHA: ${{ github.event.pull_request.base.sha }}
          GITHUB_SHA: ${{ github.sha }}
          GITHUB_EVENT_BEFORE: ${{ github.event.before }}
        run: bash scripts/ci-paths-changed.sh

  check:
    needs: changes
    runs-on: ubuntu-22.04
    timeout-minutes: 25
    steps:
      - name: Checkout
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1

      - uses: pnpm/action-setup@0977fd99725f1db4007ccb2928dbb4e90d06cc86 # v6.0.10
        with:
          version: 9

      - uses: actions/setup-node@820762786026740c76f36085b0efc47a31fe5020 # v7.0.0
        with:
          node-version: 22
          cache: pnpm

      - name: Light check (type-check + verify)
        if: needs.changes.outputs.app_source != 'true'
        run: bash scripts/ci-check-light.sh

      - uses: dtolnay/rust-toolchain@6c977a6ca4077a0ceb28ffbe03f59d46e9ac8772 # v1
        if: needs.changes.outputs.app_source == 'true'
        with:
          toolchain: stable

      - uses: Swatinem/rust-cache@6323deb102c322ba6fcbdcafc7e3dddab59af2b6 # v2.9.2
        if: needs.changes.outputs.app_source == 'true'
        with:
          workspaces: src-tauri

      - name: Linux WebKit/GTK (Tauri link — app-source diffs only)
        if: needs.changes.outputs.app_source == 'true'
        run: |
          sudo apt-get update
          sudo apt-get install -y \
            libwebkit2gtk-4.1-dev \
            libgtk-3-dev \
            libayatana-appindicator3-dev \
            librsvg2-dev \
            patchelf \
            libssl-dev \
            pkg-config

      - name: Full check (type-check + verify + Rust tests)
        if: needs.changes.outputs.app_source == 'true'
        run: bash scripts/ci-check.sh

  complexity:
    needs: changes
    if: needs.changes.outputs.app_source == 'true'
    runs-on: ubuntu-22.04
    timeout-minutes: 20
    steps:
      - name: Checkout
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1

      - name: Lizard (cyclomatic complexity, CCN ≤ 15)
        run: bash scripts/ci-complexity.sh

  crap_report:
    needs: changes
    if: needs.changes.outputs.app_source == 'true'
    runs-on: ubuntu-22.04
    timeout-minutes: 30
    steps:
      - name: Checkout
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1

      - name: Linux WebKit/GTK (Tauri link for llvm-cov)
        run: |
          sudo apt-get update
          sudo apt-get install -y \
            libwebkit2gtk-4.1-dev \
            libgtk-3-dev \
            libayatana-appindicator3-dev \
            librsvg2-dev \
            patchelf \
            libssl-dev \
            pkg-config

      - uses: dtolnay/rust-toolchain@6c977a6ca4077a0ceb28ffbe03f59d46e9ac8772
        with:
          toolchain: stable
          components: llvm-tools-preview

      - uses: Swatinem/rust-cache@6323deb102c322ba6fcbdcafc7e3dddab59af2b6
        with:
          workspaces: src-tauri

      - name: cargo-llvm-cov
        uses: taiki-e/install-action@43aecc8d72668fbcfe75c31400bc4f890f1c5853
        with:
          tool: cargo-llvm-cov

      - name: Lizard
        run: |
          source scripts/ensure-lizard.sh
          lizard --version

      - name: Seed testdata + Rust coverage JSON
        run: |
          bash scripts/seed-testdata-for-ci.sh
          cd src-tauri
          cargo llvm-cov test --lib --summary-only --json --output-path llvm-cov-summary.json -- --test-threads=1

      - name: Lizard XML (Rust)
        run: |
          source scripts/ensure-lizard.sh
          lizard src-tauri/src \
            --exclude src-tauri/src/vendor \
            -l rust \
            -X -o lizard-rust.xml

      - name: CRAP report (report-only)
        run: |
          python3 dx/crap_report.py \
            --lizard-xml lizard-rust.xml \
            --rust-cov src-tauri/llvm-cov-summary.json \
            --out-md crap-report.md

      - name: Upload CRAP artifacts
        uses: actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a
        with:
          name: crap-analysis
          path: |
            crap-report.md
            lizard-rust.xml
            src-tauri/llvm-cov-summary.json
