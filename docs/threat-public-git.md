# Public git

Tracked files stay free of live hostnames, OS home directories, Tailscale carrier-grade NAT addresses, VNC logins, and credential-shaped strings.

Name machines with the public aliases `laptop-1`, `laptop-2`, and `mac-mini`. Do not put ATS tokens or employer names in this tree.

`bash scripts/ci-fitness.sh` enforces the check.
